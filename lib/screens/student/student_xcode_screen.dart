import 'package:flutter/material.dart';

class StudentXCodeScreen extends StatelessWidget {
  const StudentXCodeScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('XCode Étudiant')),
      // ... Reste du code
    );
  }
}
