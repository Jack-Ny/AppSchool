import 'package:flutter/material.dart';

class StudentRanksScreen extends StatelessWidget {
  const StudentRanksScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Classement Étudiant')),
      // ... Reste du code
    );
  }
}
